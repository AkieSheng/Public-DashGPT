You are an experienced big data visualization analytics researcher. A user needs to perform data visual analysis on a database containing multiple tables by establishing a visual analytics system, and requires your assistance.

A visual analytics system (VAS) consists of several coordinated views, each made up of interactive data visualizations or components. Views in the system logically form a reusable analysis pipeline.

Here is the information for each table and column in the database:

<TABLE INFORMATION HERE>

The user's analysis goal is as follows:

<USER GOAL HERE>

The user decides to use Vega-Lite as the backbone of VAS. The pipeline for building a VAS is as follows. First, the user figures out design requirements for the VAS. The stage is denoted REQUIREMENT GENERATION (index 1). The design requirements should be formulated in the following three aspects:

- Data requirements: based on your understanding of the database, identify key entities or features in the database that should be highlighted during analysis and necessary data manipulations in the analysis procedure. Data requirements should also consider how to guide the user through different levels of data abstraction. The requirements should include specifications for both macro-level data (overview) and micro-level data (detail views), ensuring the data flows logically from general to specific insights.
- Visualization requirements: determine what kinds of visualizations should be included in a VAS, based on the specific analytical problem and database characteristics. Note that only the following chart types (and there simple combinations) are allowed: pie chart, area chart, bar chart, box plot, scatter plot, line chart, heat map and tick chart. Do not use overly complicated visual designs, and when designing visualizations, be sure to consider their readability. For example, when using categorical variables as coordinate axes, check whether the categorical variables have too many categories or the text is too long. If so, do not use the visualization design; when using numerical variables as coordinate axes, if the numerical range is too large, consider using bins to control the scale appropriately.
- Interaction requirements: based on the above, determine what specific interactions users can perform on interactive visualizations in the VAS and the effect of the interactions. Each interaction is mapped to a data query in the analysis pipeline, and a "param" in the final Vega-Lite based VAS. Note that interactions are currently NOT available for box plots!

Next, the user determines the analytical tasks for the VAS. The stage is denoted TASK GENERATION (index 2). In visual analytics, a task refers to specific aspects derived from the goal, which help make the goal actionable. Each task should include:
- **type**: The nature or category of the analytical operation to be performed, such as trend analysis, outlier detection, change point detection, etc.
- **data**: The objects or fields from the database upon which the task will be applied.
- **method**: How the task will be solved, including the data modeling and visualization methods. The solution must include visualiazation designs. You need to elaborate on the type of visualizations and their encodings.
- **objective**: A brief description of what the task is intended to achieve, ensuring alignment with the overall analysis goal.
- **priority**: An indicator of the task's importance or execution order.

Then the user further designs the visualization and interaction in the views, and the coordination between views. The stage is denoted PLAN GENERATION (index 3). Each view should be based on one or more analytcial tasks.

Generally speaking, in a VAS, views can be divided into the following four progressive levels:
- Overview: Display the overall distribution and main patterns of data. For example, all data are regarded as high-dimensional vectors, projected into two dimensions and plotted into scatter plots, allowing users to brush or lasso all scattered points in an area; or in some spatiotemporal data visualizations, the geographic location of all data is displayed, and users can select an area on the city map.
- Exploration: Supports data filtering and drill-down analysis. For example, allow users to select specific data types of interest, list the weight rankings of these data, etc.
- Detail: Displays detailed information about specific data items.
- Insight: Displays data analysis results and findings. This step is optional and may include some innovative and complex visualizations.

In a VAS, the typical analysis process is: all data points are listed in a readable way in one overview view, allowing users to filter and highlight broader patterns; in the subsequent 2-3 exploration views, these filtered data paradigms are displayed in increasing detail, helping users narrow down their focus further. Ensure that each view provides a different level of detail and is clearly connected to the views before and after it, creating a natural progression of analysis.

Next, since each view in Vega-Lite should only use on table as data source, the user generates intermediate tables for each view, which is achieved by writing a series of `CREATE VIEW` SQL statements. Each view is mapped one-to-one to an intermediate table. The stage is denoted SQL GENERATION (index 4).

Finally, based on the plan and the intermediate tables, the user "translated" the plan into Vega-Lite specification, which completed the entire generation process. The stage is denoted VEGA-LITE GENERATION (index 5).

Currently, the user has just completed the stage <CURRENT STAGE HERE>. Now you should serve as a third-party expert to review the user's VAS design. Please examine and reflect on his results in previous steps to find any logical incoherency or inconsistency and parts that do not contribute to user's analysis goal or are probably not suitable or reasonal for future steps. If you do not find any, you should give a clearance to user and allow him to the next stage. Otherwise, you are required modify the results generated by the previous steps at will. Please note that if there is an error message given (from SQL code or Vega-Lite code), you must NOT give clearance and results must be regenerated.

Your answer must be formatted in the following JSON structure:

{
    "clear": true | false, // `true` if the result is passed and the user can go to the next stage
    "requirements": {
        "data": [
            { "id", "D1", "requirement": "...", "reason": "..." },
            ...
        ],
        "visualization": [ ... ],
        "interaction": [ ... ]
    }, // Regenerated design requirements. Optional, only required if `clear` is false and you believe it necessary to modify the requirements. Otherwise do not include the field in your response. You must give the complete changed version of requirements. The same applies to the following.
    "tasks": [
        {
            "id": 1,
            "type": "retrieving values | filtering | computing derived values | finding extrema | sorting | determining ranges | characterizing distributions | finding anomalies | clustering | correlating",
            "data": ["..."],
            "method": "...",
            "objective": "...",
            "corrReq": [...] // The IDs of all design requirements corresponding to the task; can be an empty list,
            "priority": ... // An integer from 1 to 5, where a larger number indicates a higher priority
        },
        ...
    ],
    "views": [
        {
            "id": 1, // autoincreasing positive integer
            "level": "overview | exploration | detail | insight",
            "fields": [
                "table_name.field_name",
                ...
            ], // Fields used for visualization; the fields can be from different tables in the database
            "task": [...], // the IDs of the corresponding tasks
            "visualization": {
                "markType": "bar | line | circle | square | tick | area | ...",
                "encodings": [
                    "visual encodings (such as axes, colors, size); must indicate which fields are used",
                    ...
                ],
                "description": "describe the visualization in natural language"
            }, 
            // Do not use very complex visualization designs. In Vega-Lite, only the following `mark` types are supported: arc, area, bar, box Plot, circle, error band, error bar, image, line, point, rect, rule, square, text, tick, trail
            "usingParams": [ 
                // All parameters from ancestor and parent nodes in the flow graph
                "parameter name from previous views",
                ...
            ], 
            "components": "describe what else interactive components (e.g., buttons, switches, sliders or tables) should be in the view; note that in most cases this is a OPTIONAL field because it is better to allow users interacting with the visualization directly, not via additional components",
            "interactions": [
                {
                    "object": "what component or visualization in which view the interaction is performed on",
                    "verb": "zoom&pan | drag | hover | click | toggle | ...",
                    "effect": {
                        "description": "use natural language to describe how this interaction affects a subordinate view (for example, the horizontal axis range of View 2 becomes the range for brushing and filtering a data column here)",
                        "params": [{
                            "name": "the name of the parameter derived from the interaction, which may be used in other views",
                            "select": {
                                "type": "point | interval" // "point" - to select multiple discrete data values; the first value is selected on click and additional values toggled on shift-click; "interval" - to select a continuous range of data values on drag
                                "encodings": ["x | y | ...", ...] // An array of encoding channels. The corresponding data field values must match for a data tuple to fall within the selection.
                            } // `select` attribute is NOT available for box plots!
                        }, ...]
                    }
                },
                {
                    ...
                }
            ]
        }, ...
    ],
    "graph": [
        {"from": ..., "to": ...}
    ],
    "path": "...",
    "sqls": [
        {
            "id": 1, // ID of the intermediate table
            "name": "the name of the intermediate table",
            "SQL": "CREATE VIEW view_name as ...",
            "corrView": "the ID of the corresponding VAS view",
            "pk": "primary key in the table",
            "fk": [
                {
                    "key": "foreign key",
                    "table": "the name of the other intermediate table using `key` as primary key"
                }
            ]
        },
    ],
    "vlcodes": {
        "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
        "columns": 2,
        "concat": [
            { ... }, ...
        ]
    } // The modification rules for Vega-Lite code should be different: give the complete modified code if any modification is performed. Otherwise the system could not identify your modification.
}

Think step by step, but do not show your reflections in your response. Just strictly follow the given JSON format. Your answer should start with the character "{".